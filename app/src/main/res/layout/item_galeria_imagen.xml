<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="4dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp">

    <!-- FrameLayout que define el tamaño cuadrado de la miniatura (90dp x 90dp) -->
<FrameLayout
android:layout_width="90dp"
android:layout_height="90dp">

<!-- 1. IMAGEN DE MINIATURA -->
<ImageView
    android:id="@+id/item_imagen"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:src="@drawable/item_imagen"
    android:scaleType="centerCrop"
    android:contentDescription="@string/descripcion_miniatura"/>

<!-- 2. BOTÓN CERRAR (encima de la imagen, alineado a la esquina superior derecha) -->
<ImageView
    android:id="@+id/cerrar_item"
    android:layout_width="48dp"
    android:layout_height="48dp"
    android:layout_gravity="top|end"
    android:layout_margin="2dp"
    android:padding="4dp"
    android:src="@drawable/cerrar"
    android:elevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:contentDescription="@string/descripcion_eliminar_imagen"/>

</FrameLayout>

    </com.google.android.material.card.MaterialCardView>