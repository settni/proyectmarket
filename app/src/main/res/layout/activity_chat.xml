<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F0F0"
    tools:context=".ChatActivity">

    <!-- 1. Cabecera (Header) -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="0dp"
        android:layout_height="65dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingHorizontal="16dp"
        android:background="@color/design_default_color_primary"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Botón de Retroceso (Flecha hacia atrás) -->
        <ImageButton
            android:id="@+id/btnBack"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_back"
            android:contentDescription="Volver"
            app:tint="@color/white" />

        <!-- Foto de Perfil (Avatar) -->
        <ImageView
            android:id="@+id/imgProfile"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="16dp"
            android:src="@drawable/ic_profile_placeholder"
            android:background="@drawable/rounded_chat_header_bg"
            android:padding="4dp"
            android:contentDescription="Foto de Perfil del Contacto"/>

        <!-- Información del Contacto -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:orientation="vertical">

            <!-- Nombre del Contacto -->
            <TextView
                android:id="@+id/tvContactName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/white"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="DiegoDev" />

            <!-- Estado (Online / Última vez) -->
            <TextView
                android:id="@+id/tvContactStatus"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="#FFFFFF"
                android:textSize="12sp"
                tools:text="online" />
        </LinearLayout>

    </LinearLayout>

    <!-- 2. Área de Entrada de Mensajes (Corregida) -->
    <LinearLayout
        android:id="@+id/layout_input"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="8dp"
        android:background="@color/white"
        android:gravity="center_vertical"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Caja de Texto para escribir el mensaje (CORREGIDA LA SINTAXIS Y ALTURA) -->
        <EditText
            android:id="@+id/editTextMensaje"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:minHeight="48dp"
            android:layout_weight="1"
            android:hint="Escriba algo"
            android:maxLines="4"
            android:padding="12dp"
            android:background="@drawable/rounded_edittext_bg"
            android:imeOptions="actionDone"
            android:inputType="textCapSentences|textMultiLine"
            android:textColor="@color/black"
            android:textColorHint="#999999"
            />

        <!-- Botón para Adjuntar Imagen -->
        <ImageButton
            android:id="@+id/btnAttachImage"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Adjuntar Imagen"
            android:background="@drawable/send_button_bg_secondary"
            android:src="@drawable/ic_gallery"
            app:tint="@color/design_default_color_primary"
            android:scaleType="fitCenter"
            android:padding="12dp"/>

        <!-- Botón de Enviar -->
        <ImageButton
            android:id="@+id/btnEnviar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Botón Enviar Mensaje"
            android:background="@drawable/send_button_bg"
            android:src="@drawable/ic_send"
            app:tint="@color/white"
            android:scaleType="fitCenter"
            android:padding="12dp"/>

    </LinearLayout>

    <!-- 3. RecyclerView para mostrar los mensajes (Fondo gris claro) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewChat"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#F0F0F0"
        android:paddingTop="8dp"
        android:clipToPadding="false"
        app:layout_constraintTop_toBottomOf="@id/layout_header"
        app:layout_constraintBottom_toTopOf="@id/layout_input"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:listitem="@layout/item_mensaje"
        tools:itemCount="20"/>

</androidx.constraintlayout.widget.ConstraintLayout>